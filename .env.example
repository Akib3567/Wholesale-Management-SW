# Copy to .env and fill in. Never commit .env.

# --- apps/server (dev runner) ---
# Path to this install's SQLite database file
ERP_DB_PATH=./shop.sqlite
# Local API port (server binds 127.0.0.1 only — never exposed to the network)
ERP_PORT=3001
